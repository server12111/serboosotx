services:
  bot:
    build: .
    restart: always
    env_file: .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    command: sh -c "alembic upgrade head && python run.py"
