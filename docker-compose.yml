services:
  bot:
    build: .
    restart: always
    env_file: .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    # run.py applies pending migrations itself on startup — no wrapper command needed.
